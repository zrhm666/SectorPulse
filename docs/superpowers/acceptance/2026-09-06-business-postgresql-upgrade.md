# 2026-09-06 本机业务 PostgreSQL 升级验收

## 结论

用户启动本机 PostgreSQL 服务并授权继续后，已完成 `sectorpulse_runtime` 业务库的迁移前备份、014→018 前向迁移，以及真实应用启动和只读 HTTP 查询验收。不需要 Docker。

本轮使用主目录 `.venv` 和 `main` 上已交付的应用代码（基线 `37a89e2`），没有修改应用源码、数据库连接配置或授权文件，也没有创建测试运行、调用行情/新闻/LLM 上游、恢复影子测试。

## 连接与备份

- PostgreSQL 18.6，本机服务 `postgresql-x64-18`，端口 5432。
- 连接目标：`localhost:5432/sectorpulse_runtime`；角色 `sectorpulse_app`，凭据读取项目 `.env`，不写入文档或日志。
- 迁移前版本：001–014；检查时无其他数据库客户端连接。
- 备份：`D:\work\SectorPulse\backups\postgresql-20260906-004732-before-018\sectorpulse_runtime.dump`。
- 格式：PostgreSQL custom-format；大小 605,046 字节；归档目录 163 项。
- SHA-256：`639afff676395522d6d3e9b3eaf95678548ba7e950b91562e042f2106ca1dd15`。
- `pg_dump` 成功，`pg_restore --list` 成功，全部归档内容转换为 SQL 并丢弃输出的读取检查成功；迁移前再次校验文件哈希。
- 该备份包含业务数据，仅保留在被 Git 忽略的 `backups/`。本轮没有将业务备份实际恢复到另一数据库；不能将归档读取验证说成完整恢复演练。

## 迁移与数据保留

调用现有 `PostgresDatabase.initialize()`，依次应用：

1. 015：候选板块选择记录表及索引。
2. 016：任务恢复、重试来源、取消请求、心跳及调度字段；PostgreSQL 状态约束和索引。
3. 017：内容运行状态约束加入 `INTERRUPTED`。
4. 018：内容运行重试来源字段及索引。

迁移后记录为连续的 001–018，数据库健康检查通过。对迁移前 43 张业务表的 6,499 条记录，按原有列计算逐行哈希再汇总比较，迁移前后完全一致。新建候选选择表为空，旧记录新增重试来源字段为空。后续两次正常应用启动均跳过已执行迁移，没有重复字段或约束错误。

## 启动恢复与真实接口

使用绑定随机回环端口的真实 Uvicorn/FastAPI 服务，连接业务 PostgreSQL。验证进程临时停用定时派发、使用 Fixture LLM 配置；确认任务没有关联数据运行，避免后台推进生成任务。只发出 GET 请求，不执行生成、批准、导出或其他业务写入。

最终 81 个 HTTP 请求均返回 200，覆盖：

- 已构建首页、JS/CSS 资源、健康检查、运营总览。
- 内容运行列表及全部 21 条详情、现有一条可查询草稿视图。
- 数据运行列表及全部 29 条详情。
- 全部 18 条任务详情、定时任务列表。
- 带明确时间区间的审核统计、影子历史及摘要、提示词黄金案例列表。

正常启动恢复逻辑将此前遗留的 15 条内容运行由 `RUNNING` 标为 `INTERRUPTED`，补充结束时间与重启中断说明。原有 5 条 FAILED、1 条 UNREVIEWED 保持不变；这不是新一轮生成失败，也没有自动重跑。

启动前后所有表的行数相同。直接读取备份内 `phase1b_runs` 的 COPY 数据并与当前数据库比较，确认除上述恢复字段（status、finished_at、error_message）外的原字段完全一致；其他业务表与迁移前备份的哈希相同。没有删除新闻、草稿或运行历史。

验证脚本首次请求审核统计时漏传 `from_at/to_at`，接口按约定返回 422；补齐验证请求参数后全部通过，不涉及应用代码修复。第一次启动已执行上述恢复，第二次启动未新增恢复操作，验证了恢复的幂等性。

验证用临时后端已正常关闭；PostgreSQL 服务继续运行。未留下额外占用 8000/9000 的验证进程。用户可按原有启动流程运行业务后端。

## 收尾回归

本次再次执行 `scripts/verify-stage0.ps1`，完整质量脚本退出码为 0：Ruff、163 个源文件 Mypy、Python 依赖审计、非 Live 后端测试、前端单测、生产构建、真实 SQLite HTTP 流程、40 项浏览器测试及 npm 生产依赖审计通过。后端结果为 343 通过、22 个 PostgreSQL 测试跳过、7 个 Live 测试排除。常规回归强制使用临时 SQLite，没有把业务 PostgreSQL 当作测试库。

含开发依赖的 npm 安全告警不属于生产依赖审计结论，仍是单列升级待办。本轮文档更新在现有 `codex/stage0-reliability` 分支记录，并同步本地 main；不推送远端，不改动已验证的应用代码。

## 本地证据与范围

备份目录同时保留：

- `backup-manifest.json`：备份校验信息、旧列清单和数据哈希。
- `migration-verification.json`：迁移版本和原数据完整性结果。
- `startup-http-results.json`：最终 81 个查询的状态码。
- `startup-verification.json`：启动恢复、数据保留与关闭检查结果。

操作脚本保留于主目录 `.tmp/`，不进入 Git。不能把本轮真实库 GET 查询验收等同于 Live 数据获取、真实 LLM 生成或人工批准流程的本轮全链路验收。开发工具链安全升级、可选 Docker 启动和暂停中的影子验收仍沿用原计划边界。
